# Order Management API

REST API сервис для добавления товаров в заказы. 
Сервис позволяет добавлять товары в существующие заказы с автоматическим увеличением 
количества, если товар уже присутствует в заказе.


## 🚀 Возможности

- ✅ **Добавление товара в заказ** через REST API
- ✅ **Автоматическое увеличение количества** - если товар уже есть в заказе, его количество увеличивается, а не создается новая позиция
- ✅ **Проверка наличия товара** - система проверяет достаточность товара на складе перед добавлением
- ✅ **Валидация данных** - автоматическая валидация входных параметров через Pydantic
- ✅ **Система логирования** - подробное логирование всех операций в консоль
- ✅ **Автоматическая документация** - Swagger UI (`/docs`) и ReDoc (`/redoc`)
- ✅ **Типизация** - полная типизация с использованием Pydantic и SQLAlchemy

## 📋 Требования

- Python 3.12+
- PostgreSQL 15+
- Git (для клонирования репозитория)

## 🛠 Установка и запуск

## Клонирование репозитория

*Для создания локальных настроек, необходимы права администратора* 

1. **Создаем папку проекта**

*Необходим указать желаемый путь создания клона проекта, например C:\rest_api_adding_order*

```bash
md C:\rest_api_adding_order
```
2. **Клонируйте репозиторий из GitHub:**

```bash
cd C:\  
# если путь не корневой, надо путь до папки rest_api_adding_order, 
# как пример cd C:\Temp (если в предыдущем пункте 
# создали папку C:\Temp\rest_api_adding_order)
git clone https://github.com/e-kl-tech/rest_api_adding_order.git
cd rest_api_adding_order
```

Или если репозиторий уже склонирован:
```bash
cd path_to_folder_project
```

### Установка и настройка

1. **Использовать docker**

   1.1 Проверить наличие Docker.

   ```bash
   docker -v
   # Docker version 28.5.2, build ecc6942
   ```
   *Установить Docker, если отсутствует.*

   1.3 Запустить проект.

   *Убедиться что вы в папке проекта или перейти нее*

   ```bash
   # На Windows:
   dir
      
   # На Linux/Mac:
   ls -l
   
   cd path_to_folder_project
   
   docker-compose up -d --build
   ```
   
   

2. **Локальный запуск**

   2.1 Установите PostgreSQL и создайте базу данных
   ```bash
   psql -U postgres  # создается в процессе установки
   ```

   ```sql
   \l  --покажет все таблицы
   CREATE DATABASE orders_db;
   \с orders_db;  # если нету orders_db, создать
   \i path_to_folder_project/init-db/init.sql
   ```
   2.2 Поменять настройки в файле setting.yaml
   ```
   database_url: postgresql+psycopg2://postgres@db:5432/orders_db
   заменить db на localhost
   database_url: postgresql+psycopg2://postgres@localhost:5432/orders_db
   ```

   2.2 **Создайте виртуальное окружение:**

   ```bash
   python -m venv venv
   
   # На Windows:
   venv\Scripts\activate
   
   # На Linux/Mac:
   source venv/bin/activate
   ```

   2.3 **Установите зависимости:**

   ```bash
   pip install -r requirements.txt
   ```

   2.4 **Запустите приложение:**

   ```bash
   uvicorn app.main:app --reload
   ```
3. **Приложение будет доступно по адресу:**
   - API: `http://localhost:8000`
   - Swagger документация: `http://localhost:8000/docs`
   - ReDoc документация: `http://localhost:8000/redoc`



## API Документация

### POST `/api/orders/add-item`

*Добавляет товар в заказ. Если товар уже есть в заказе, увеличивает его количество.*

**Request Body:**
```json
{
  "order_id": 1,
  "product_id": 5,
  "quantity": 3
}
```

**Параметры:**
- `order_id` (int, обязательный) - ID существующего заказа
- `product_id` (int, обязательный) - ID товара из справочника номенклатуры
- `quantity` (int, обязательный, > 0) - Количество товара для добавления

**Response 200 (Success):**
```json
{
  "id": 1,
  "order_id": 1,
  "product_id": 5,
  "product_name": "Название товара",
  "quantity": 3,
  "unit_price": "1000.00",
  "total_price": "3000.00"
}
```

**Ошибки:**

- **404 Not Found** - Заказ или товар не найдены
```json
{
  "detail": "Заказ с ID 1 не найден"
}
```

- **400 Bad Request** - Недостаточно товара на складе
```json
{
  "detail": "Недостаточно товара на складе. Доступно: 5, требуется: 10"
}
```

### Другие endpoints

- `GET /` - Информация об API
- `GET /health` - Проверка здоровья сервиса

## 🔍 Логика работы

1. **Проверка существования заказа** - система проверяет, существует ли заказ с указанным ID
2. **Проверка существования товара** - система проверяет наличие товара в справочнике
3. **Проверка наличия товара на складе** - учитывается текущее количество товара в заказе (если товар уже добавлен) и запрашиваемое количество
4. **Обновление или создание позиции**:
   - Если товар уже есть в заказе → увеличивается `quantity` существующей позиции
   - Если товара нет в заказе → создается новая запись в `order_items`
5. **Возврат результата** - возвращается информация о позиции заказа

## 🗂 Структура проекта

```
rest_api_adding_order/
├── init-db/
    ├── init.sql                # Создание пустых таблиц
├── app/
│   ├── __init__.py
│   ├── main.py                 # Точка входа FastAPI
│   ├── database.py             # Подключение к БД и настройки
│   ├── models.py               # SQLAlchemy модели БД
│   ├── schemas.py              # Pydantic схемы для валидации
│   ├── services.py             # Бизнес-логика
│   └── api/
│       └── routes/
│           └── orders.py       # REST API endpoints
├── requirements.txt            # Python зависимости
├── config.py                   # Настройки проекта
├── setting.yaml                # Настройки проекта
├── Dockerfile                  # Docker образ приложения
├── docker-compose.yml          # Docker Compose конфигурация
├── .gitignore                  # Git ignore файл
└── README.md                   # Документация
```

## 🧪 Тестирование

Интерактивное тестирование доступно через Swagger UI: `http://localhost:8000/docs`

### Пример запроса через Python:

```python
import requests

url = "http://localhost:8000/api/orders/add-item"
data = {
    "order_id": 1,
    "product_id": 5,
    "quantity": 3
}

response = requests.post(url, json=data)
print(response.json())
```

**Минимальный набор для тестирования API:**
- Товары (`products`) - минимум один товар с `quantity > 0`
- Заказы (`orders`) - минимум один заказ

## 📝 Технологии

- **Python 3.11** - язык программирования
- **FastAPI** - современный веб-фреймворк для создания API
- **SQLAlchemy 2.0** - ORM для работы с БД
- **PostgreSQL** - реляционная база данных
- **Pydantic** - валидация данных
- **Logging** - система логирования Python
- **Uvicorn** - ASGI сервер

## 📄 Лицензия

Этот проект создан в рамках тестового задания.

## 👤 Автор

Создал Evgenii Klimov для Aiti Guru
